# my-python-app-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: my-python-app-service
  labels:
    app: my-python-app
spec:
  type: NodePort
  selector:
    app: my-python-app
  ports:
  - protocol: TCP
    port: 8501
    targetPort: 8501