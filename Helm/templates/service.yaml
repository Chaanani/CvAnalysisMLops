apiVersion: v1
kind: Service
metadata:
  name: my-python-app-service
  labels:
    app: my-python-app
spec:
  type: NodePort
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
  selector:
    app: my-python-app

